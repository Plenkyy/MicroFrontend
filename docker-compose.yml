version: '3.8'
services:
  service1:
    build:
      context: ./MainSite
      dockerfile: Dockerfile
    ports:
      - 8081:80

  service2:
    build:
      context: ./reporting_host_mf
      dockerfile: Dockerfile
    ports:
      - 8082:80
